const numeros = [54, 22, 14, 87, 10, 284];

for (let i = 0; i < numeros.length; i++) {
    if (numeros[i] === 10) {
        console.log(`O número 10 está na posição ${i} do array`);
        break;
    }
}

